name: Keep App Awake
on:
  schedule:
    - cron: '*/30 * * * *' # Εκτέλεση κάθε 30 λεπτά
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: curl -I https://your-app-name.streamlit.app # Το link της εφαρμογής σου
